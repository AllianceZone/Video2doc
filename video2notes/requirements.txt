# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0

# --- Auth ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4's backend-detection self-test breaks against bcrypt>=4.0's
# changed internals (raises "password cannot be longer than 72 bytes" even
# for short passwords, during its own startup self-test) -- pin below 4.0
# until passlib ships a fix, or switch hash_password/verify_password in
# api/security.py to call bcrypt directly if you need bcrypt>=4.
bcrypt<4.0

# --- Database ---
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
alembic>=1.13.0

# --- Async jobs ---
celery>=5.4.0
redis>=5.0.4

# --- Object storage ---
boto3>=1.34.0

# --- Pipeline: video/audio/frames ---
yt-dlp>=2024.8.6
gdown>=5.2.0
requests>=2.31
opencv-python-headless>=4.9.0.80
Pillow>=10.3.0
scenedetect>=0.6.4
imagehash>=4.3.1
numpy>=1.26

# --- Pipeline: transcription ---
faster-whisper>=1.0.3
openai>=1.35.0

# --- Pipeline: summarization (local provider) ---
scikit-learn>=1.4.0
networkx>=3.2

# --- Pipeline: document generation ---
python-docx>=1.1.2
python-pptx>=1.0.2

# --- Testing ---
pytest>=8.2.0
httpx>=0.27.0
moto[s3]>=5.0.0
